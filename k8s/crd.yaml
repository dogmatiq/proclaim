apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: dnssd-service-instances.proclaim.dogmatiq.io
  labels:
    app.kubernetes.io/name: proclaim.dogmatiq.io
    app.kubernetes.io/part-of: proclaim
spec:
  scope: Namespaced
  group: proclaim.dogmatiq.io
  names:
    plural: dnssd-service-instances
    singular: dnssd-service-instance
    kind: DNSSDServiceInstance
    categories:
      - dnssd
  versions:
    - name: v1alpha1
      served: true
      storage: true
      subresources:
        status: {}
      additionalPrinterColumns:
        - name: Instance Name
          type: string
          description: The name of the DNS-SD service instance
          jsonPath: .spec.name
        - name: Service Type
          type: string
          jsonPath: .spec.service
        - name: Domain
          type: string
          description: The domain number under which the DNS-SD service instance is advertised
          jsonPath: .spec.domain
        - name: Target Host
          type: string
          jsonPath: .spec.targetHost
        - name: Target Port
          type: integer
          jsonPath: .spec.targetPort
        - name: Provider
          type: string
          description: The provider used to publish the DNS-SD service instance
          jsonPath: .status.providerDescription
      schema:
        openAPIV3Schema:
          type: object
          required:
            - spec
          properties:
            spec:
              type: object
              required:
                - name
                - service
                - domain
                - targetHost
                - targetPort
              properties:
                name:
                  type: string
                service:
                  type: string
                domain:
                  type: string
                targetHost:
                  type: string
                targetPort:
                  type: integer
                  format: uint16
                priority:
                  type: integer
                  format: uint16
                weight:
                  type: integer
                  format: uint16
                ttl:
                  type: integer
                  format: uint16
                  default: 60
                attributes:
                  type: array
                  items:
                    type: object
                    additionalProperties:
                      type: string
            status:
              type: object
              properties:
                providerId:
                  type: string
                providerDescription:
                  type: string
                advertiserId:
                  type: string
